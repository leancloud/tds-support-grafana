apiVersion: 1

datasources:
  - name: Ticket
    type: elasticsearch
    access: proxy
    database: 'ticket'
    url: $ES_URL
    basicAuth: true
    basicAuthUser: $ES_USERNAME
    basicAuthPassword: $ES_PASSWORD
    jsonData:
      timeField: 'createdAt'
